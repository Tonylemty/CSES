### 題目意思
* 輸入行和列，並找出矩陣中該位置的值為何

### 核心概念
* 找規律：
  * 第 $n$ 層的最大值 = $n^2$
  * 奇數層從**右下**開始往下走
  * 偶數層從**左上**開始往上走

* 根據 `(x, y)` 找「層數」n：
  * `n = max(x, y)`，表示在第 `n` 層的外框

* 推導座標在該層的位置：
  * $n$ 是奇數 &rarr; 往下、往左
  * $n$ 是偶數 &rarr; 往上、往右

### 問題點
1. 有看出規律，但推導不出公式
2. 沒有發現「第 $n$ 層的最大值 = $n^2$」