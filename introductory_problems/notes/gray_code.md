### 題目意思
* 給定一個整數 $n$，要列出 所有長度為 $n$ 的二進位字串，總共會有 $2^n$ 個
* 排列順序必須滿足：相鄰兩個字串，恰好只有一個 bit 不同

### 核心觀念
* Gray Code 固定結構：
  * $n$ 位 Gray Code 
  = $(n - 1)$ 位 Gray Code 前面加 0 + $(n - 1)$ 位 Gray Code 前面加 1

### 問題點
* 不知道 Gray Code 的性質
* 不知道怎麼構造 Gray Code 的結構