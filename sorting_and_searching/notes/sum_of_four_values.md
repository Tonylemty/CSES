### 題目意思
* 給定一個含有 $n$ 個整數的陣列，以及一個目標總和 $x$
* 從陣列中找出四個不同位置的數字，使它們的和恰好等於 $x$
* 輸出要求：
  1. 輸出這四個數字在原始輸入中的位置（1-indexed）
  2. 如果有多組解，輸出任何一組即可
  3. 如果找不到任何組合，輸出 `IMPOSSIBLE`

### 核心概念
* 預處理：紀錄位置和排序
  * 使用 `pair` 或是 `struct` 來記錄原數字的位置
  * 對容器中的元素進行排序
* 雙指針解法：
  * 先分別使用兩個 `for` 迴圈固定兩個數
  * 使用雙指針進行解題
