### 題目意思
* 給定一個長度為 `n` 的陣列
* 再給一個目標值 `x`
* 找出兩個不同位置的元素，使得 `a[i] + a[j] = x` 且 `i != j`
* 若找不到輸出 `IMPOSSIBLE`

### 核心概念
* 常見解法：
  1. Hash，但是在 CSES 可能會被測資卡
  2. 排序 + 雙指針
* 資料前處理：將每個數字和「原始位置」綁在一起，並存入到陣列當中