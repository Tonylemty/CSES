### 題目意思
* 給定一個長度為 `n` 的陣列
* 要求計算**連續**子陣列中，每個元素**都不重複**的子陣列數量

### 核心概念
* 使用雙指標 `l` 和 `r` 維護一個 `[l ... r]` 的區間
* 使用 `map/set` 紀錄出現次數
* 視窗擴展的規則：
  右指標向右掃
  * 沒重複：直接加入
  * 有重複：移動左指標 `l++`，直到沒有重複
* 答案公式：`ans = (r - l + 1)`
  * 其中 `r - l + 1`，指的是以 `r` 結尾、且不重複的連續子陣列

