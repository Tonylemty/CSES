### 題目意思
* 依序放方塊，每座塔上面一定要比下面小
* 求最少幾座塔

### 核心概念
* 每座塔 = 一個遞減序列
* 不管塔裡怎麼疊，只記每座塔的頂部 `top = {每座塔的頂部}`
* 貪心原則：當新方塊 `x` 來時，將 x 放在**頂部 > `x` 且最接近 `x` 的位置**
* 操作流程：
  1. 使用 `upper_bound(x)` 找出第一個大於 `x` 的位置
  2. 判斷：
    - 找到（`it != end`）：把該塔頂替換成 `x`
    - 沒找到（`it == end`）：開新塔，將 `x` 加入到 `top` 中

